<div>
    <a href="/">Still procrastinating?</a>
    <br><br>
</div>

<h4>
    <a href="https://www.theonion.com{url}}">{{heading}}</a>
</h4>

<p>
    <img src="{{img_url}}">
    <br><br>
    {{description}}
</p>
<p class="date">{{date}}</p>

<hr>
<form class="form" action="/news/{{_id}}/comments" method="POST">
    <div class="form-group">
        <label for="newComment">Comments? Questions?:</label>
        <textarea name="text" id="newComment" class="form-control" Your thoughts mean so much...></textarea>
    </div>
    <input type="submit" value="Comment" class="btn btn-submit">
</form>

<br>
<h4>{{comments.length}} What Our Readers Think</h4>
{{#each comments}}
    <div class="comment">
        <p class="date">{{date}}</p>
        <button class="btn btn-danger" style="float: right" onclick="deleteComment('{{../_id}}', '{{_id}}')">LOL, nevermind.</button>
        <p>{{text}}</p>
    </div>
{{/each}}